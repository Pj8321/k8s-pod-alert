name: Build and Push Docker Image

on:
  push:
    branches: [ main ]

permissions:
  contents: write
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - name: Setup Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.21'

    - name: Regenerate go.sum for Linux
      run: |
        rm -f go.sum
        go mod tidy
        go mod verify

    - name: Commit updated go.sum
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        if ! git diff --quiet go.sum; then
          git add go.sum
          git commit -m "chore: auto-regenerate go.sum for Linux [skip ci]"
          git push
        fi
      continue-on-error: true
      if: github.event_name == 'push'

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64
        push: true
        tags: |
          ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.IMAGE_NAME }}:latest
          ${{ secrets.DOCKERHUB_USERNAME }}/${{ secrets.IMAGE_NAME }}:${{ github.run_number }}
        no-cache: true
```

## Key Changes Based on Your Secrets:

1. **Docker Hub Username**: `${{ secrets.DOCKERHUB_USERNAME }}`
2. **Docker Hub Token**: `${{ secrets.DOCKERHUB_TOKEN }}`
3. **Image Name**: `${{ secrets.IMAGE_NAME }}`

## Your Docker Image Tags Will Be:
```
<your-dockerhub-username>/<image-name>:latest
<your-dockerhub-username>/<image-name>:<build-number>
